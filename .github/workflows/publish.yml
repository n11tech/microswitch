name: Publish to GitHub Packages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      skipTests:
        description: 'Skip tests during publish'
        required: false
        default: 'true'

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      GPR_OWNER: ${{ github.repository_owner }}
      # Extract repo name from owner/repo
      GPR_REPO: ${{ github.event.repository.name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'
          server-id: 'github'                   # matches distributionManagement <id>
          server-username: GITHUB_ACTOR         # env var provided by Actions
          server-password: GITHUB_TOKEN         # env var provided by Actions

      - name: Extract version from pom.xml
        id: extract_version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Build and publish
        run: |
          mvn -B $([[ "${{ github.event.inputs.skipTests }}" != 'false' ]] && echo '-DskipTests') \
            -Dgpr.owner=${GPR_OWNER} \
            -Dgpr.repo=${GPR_REPO} \
            clean deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish summary
        run: |
          VERSION="${{ steps.extract_version.outputs.VERSION }}"
          echo "✅ Successfully published microswitch version $VERSION to GitHub Packages"
          echo "📦 Package URL: https://github.com/${{ github.repository_owner }}/microswitch/packages/"
